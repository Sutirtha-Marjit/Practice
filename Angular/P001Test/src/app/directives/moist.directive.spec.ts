import { MoistDirective } from './moist.directive';

describe('MoistDirective', () => {
  it('should create an instance', () => {
    const directive = new MoistDirective();
    expect(directive).toBeTruthy();
  });
});
